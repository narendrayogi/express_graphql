# GraphQL project with TS, TypeORM and type-graphql

Steps to run this project:

1. Run `npm i --f` command
2. Setup database settings inside `data-source.ts` file
3. Run `npm run graphql_dev` command
